# postgresx 上下文

本文件定义 `postgresx` 与其使用方共享的核心词汇。它只记录领域含义与能力边界，
不记录具体实现、API 签名、存储或部署决定。

## 角色与边界

**适配器**：把 PostgreSQL 的 wire 协议（连接、SQL 执行、COPY、TLS）收敛成稳定 Rust API 的库；
它不承载领域模型、不做业务表结构假设，也不负责多数据源编排。
_Avoid_: ORM / 查询构建器（本仓库不生成 SQL，也不把表映射为类型）

**配置**：构造期一次性校验、之后只读的声明式参数集合；校验失败即拒绝构造，
不提供「先构造、后补验」的路径。
_Avoid_: 运行时设置（本仓库没有可变更的运行时配置面）

**连接池**：持有至多 `max_pool_size` 条物理连接、以 `acquire_timeout` 约束等待的并发闸门；
它是可 `Clone` 的共享句柄（内部 `Arc`），克隆不复制连接。
_Avoid_: 全局单例（池的共享范围由调用方持有句柄决定，不是进程级隐含状态）

**连接句柄**：一次具体的会话上下文（`PgConnection`），承载参数化 SQL 与 COPY 原语；
它属于某个事务或某次借用，不独立决定重试或生命周期。
_Avoid_: 客户端（本仓库的入口是池与连接句柄，不存在聚合式的「客户端」对象）

## SQL 与事务

**参数化 SQL**：值经 `$N` 占位符 + `ToSql` 绑定、SQL 文本由调用方以常量给出的执行方式；
它是本仓库查询入口的唯一形态。
_Avoid_: 预编译语句（本仓库不暴露 prepare 句柄，也不承诺服务端侧缓存语义）

**COPY 载荷**：经 `COPY IN` / `COPY OUT` 传输的单次字节体，受 16 MiB 上限常量约束；
它是批量数据通路，不是查询结果的替代品。
_Avoid_: 批量插入（`COPY` 的语句名由调用方给出，边界与语义比一般插入更窄）

**事务状态机**：`TxStatus` 对事务句柄所处阶段的准确刻画
（`Active` / `Committed` / `RolledBack` / `Failed`）；它描述已发生的事实，不猜测意图。
_Avoid_: 连接状态（事务状态回答「这次事务到哪一步了」，与底层连接是否健康是两件事）

**脱池**：连接在超时或任务取消时被丢弃而非归还池中，以避免复用状态未知的会话。
_Avoid_: 重连（脱池是丢弃本次会话，不承诺立即建立新连接）

## 迁移与版本

**迁移**：带序号、名称与 SQL 文本的一条版本化 schema 变更（`Migration`），
其身份由 checksum 固定；它不是「任意时刻可重放的脚本」。
_Avoid_: schema 同步（本仓库只按已登记迁移推进，不做目标态推导）

**校验与执行分离**：`verify()` 只读地校验 checksum 与未知版本、绝不执行 DDL，
执行 pending 必须显式调用 `apply()`。
_Avoid_: 自动迁移（启动默认入口是 `verify()`，不存在「启动即升级」的隐式行为）

**迁移锁**：以 `pg_advisory_xact_lock` 在数据库侧取得的互斥，保证多实例并发启动时迁移串行；
它随事务提交 / 回滚自动释放。
_Avoid_: 应用锁（该互斥由数据库持有，跨进程生效，不是进程内锁）

**fail-closed**：一旦发现 checksum 不匹配或存在未知版本，就以拒绝启动收场
（`ensure_boot_ok`），而不是降级继续。
_Avoid_: 容错启动（此处不容错，宁可不启动也不接受版本不确定的库）

## 错误与可靠性

**SQLSTATE 语义分类**：把服务端返回的 SQLSTATE 归入稳定的 `ErrorKind`
（如缺失、唯一冲突等），供调用方按语义而非字面错误码分支。
_Avoid_: 错误码透传（分类是为了让判定与 PostgreSQL 版本解耦）

**可重试错误**：调用方可以安全重复施加该操作的那类失败；它与「永久错误」互为补集，
判定走 `is_retryable()`。
_Avoid_: 网络错误（网络只是成因之一，不能表达「重试是否安全」这一判据）

**重试预算**：以总 deadline 约束的重试上限，退避为指数增长并叠加抖动；
超出预算即放弃，不再尝试。
_Avoid_: 无限重试 / 固定间隔（前者无界，后者在抖动场景下会同步重试风暴）

**TLS 严格模式**：非 loopback 主机在 `validate()` 阶段被强制 `sslmode=require`，
rustls 始终校验服务端证书，无 insecure 旁路。
_Avoid_: 加密开关（它不是可选项，而是一条不可绕过的准入条件）
